---
# Copy this file to vault/secrets.yml, set real values, then run:
# ansible-vault encrypt vault/secrets.yml

vault_penpot_database_password: "CHANGE_ME"
vault_penpot_secret_key: "CHANGE_ME_LONG_RANDOM_SECRET"
